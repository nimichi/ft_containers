#pragma once

#include <memory>
#include <vector>
#include <iterator>

namespace ft {
	template <class T>
	class vector_iterator{
		std::vector<int>::iterator::difference_type;
		std::iterator_traits<int>;
		int::value
	};
}
